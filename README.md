# InterKnot_Auth_ForMac

InterKnot Auth for macOS is a native SwiftUI client for Guangdong ESurfing campus network authentication.

## Build

```sh
swift build
make bundle
```

The bundled app is generated at `InterKnotAuth.app`.
